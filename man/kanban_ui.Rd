% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kanban.R
\name{kanban_ui}
\alias{kanban_ui}
\title{Collaborative kanban board UI}
\usage{
kanban_ui(
  id,
  columns = c(todo = "To Do", in_progress = "In Progress", done = "Done"),
  column_colors = c(todo = "#64748b", in_progress = "#3b82f6", done = "#22c55e")
)
}
\arguments{
\item{id}{Module ID.}

\item{columns}{Named character vector defining the columns. Names are
internal IDs, values are display labels. Default is

\code{c(todo = "To Do", in_progress = "In Progress", done = "Done")}.}

\item{column_colors}{Named character vector of CSS colors for column headers.
Names should match column IDs. Default provides red/yellow/green styling.}
}
\value{
A Shiny UI element.
}
\description{
Creates a kanban board that synchronizes across Shiny sessions using
Automerge CRDT, without requiring an external sync server.
}
\examples{
if (interactive()) {
  ui <- shiny::fluidPage(kanban_ui("board"))
  server <- function(input, output, session) kanban_server("board")
  shiny::shinyApp(ui, server)
}

}
\seealso{
Other kanban: 
\code{\link{kanban_server}()}
}
\concept{kanban}
