% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/replay.R
\name{replay_server}
\alias{replay_server}
\title{Replay timeline server}
\usage{
replay_server(
  id,
  doc_id = "default",
  replaying,
  show_messages = TRUE,
  playback_ms = 1000
)
}
\arguments{
\item{id}{Module ID (must match the ID used in \code{\link[=replay_ui]{replay_ui()}}).}

\item{doc_id}{Document identifier. Must match the \code{doc_id} used in
\code{\link[=sync_inputs]{sync_inputs()}}. Default is \code{"default"}.}

\item{replaying}{The \code{reactiveVal} returned by \code{\link[=sync_inputs]{sync_inputs()}}.}

\item{show_messages}{Whether to display commit messages. Default is \code{TRUE}.}

\item{playback_ms}{Interval in milliseconds between steps during animated
playback. Default is \code{1000}.}
}
\value{
Called for side effects. Returns \code{NULL} invisibly.
}
\description{
Server logic for the replay timeline module. Reads the change history from
the master Automerge document created by \code{\link[=sync_inputs]{sync_inputs()}}, reconstructs
snapshots at each step, and pushes the values to widgets.
}
\examples{
if (interactive()) {
  ui <- shiny::fluidPage(
    shiny::selectInput("dist", "Distribution",
      c("Normal", "Uniform", "Exponential")),
    shiny::sliderInput("n", "Observations", 10, 500, 100),
    shiny::plotOutput("plot"),
    replay_ui("timeline")
  )
  server <- function(input, output, session) {
    replaying <- sync_inputs()
    replay_server("timeline", replaying = replaying)
    output$plot <- shiny::renderPlot(
      hist(switch(input$dist,
        Normal = rnorm(input$n),
        Uniform = runif(input$n),
        Exponential = rexp(input$n)
      ))
    )
  }
  shiny::shinyApp(ui, server)
}

}
\seealso{
Other sync:
\code{\link[=replay_ui]{replay_ui()}},
\code{\link[=sync_inputs]{sync_inputs()}}
}
\concept{sync}
