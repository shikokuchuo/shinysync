% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kanban.R
\name{kanban_server}
\alias{kanban_server}
\title{Collaborative kanban board server}
\usage{
kanban_server(
  id,
  doc_id = "default",
  columns = c(todo = "To Do", in_progress = "In Progress", done = "Done"),
  initial_items = NULL
)
}
\arguments{
\item{id}{Module ID (must match the ID used in \code{\link[=kanban_ui]{kanban_ui()}}).}

\item{doc_id}{Document identifier. All kanban boards with the same \code{doc_id}
synchronize together. Default is \code{"default"}.}

\item{columns}{Named character vector defining the columns (must match UI).}

\item{initial_items}{Optional named list of initial items per column.
Names should be column IDs, values are character vectors of item texts.}
}
\value{
A reactive expression returning a data frame with columns \code{id},
\code{text}, \code{done}, and \code{column}.
}
\description{
Server logic for a collaborative kanban board that synchronizes across Shiny
sessions using Automerge CRDT.
}
\details{
Items can be moved between columns using the arrow buttons. The kanban
board uses a single Automerge document where each item has a \code{column}
field indicating which column it belongs to.
}
\examples{
if (interactive()) {
  ui <- shiny::fluidPage(kanban_ui("board"))
  server <- function(input, output, session) {
    kanban_server("board", initial_items = list(
      todo = c("Design feature", "Write tests"),
      in_progress = c("Code review"),
      done = c("Deploy v1.0")
    ))
  }
  shiny::shinyApp(ui, server)
}

}
\seealso{
Other kanban:
\code{\link[=kanban_ui]{kanban_ui()}}
}
\concept{kanban}
