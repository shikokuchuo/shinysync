% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/edit.R
\name{project_edit}
\alias{project_edit}
\title{Edit a synced document live in a Shiny code editor}
\usage{
project_edit(doc, at = "text", ext = NULL, debounce = 300L)
}
\arguments{
\item{doc}{An \code{autosync_doc} handle backed by an active connection (from
\code{\link[autosync:sync_client]{autosync::sync_client()}}'s \verb{$open_doc()} or \code{\link[=project_open]{project_open()}}'s \verb{$open()}).}

\item{at}{Character path to the text object within the document. A single
string addresses a top-level key; a character vector navigates nested
objects with \code{[[}. Default \code{"text"}.}

\item{ext}{File extension (e.g. \code{".md"}) used to pick the editor's
syntax-highlighting language, or \code{NULL} for plain text.}

\item{debounce}{Milliseconds to wait after the last keystroke before pushing.}
}
\value{
Invisibly returns \code{doc}.
}
\description{
Opens the text object at \code{at} within an \code{autosync_doc} handle (from
\code{\link[autosync:sync_client]{autosync::sync_client()}}'s \verb{$open_doc()}, or \code{\link[=project_open]{project_open()}}'s \verb{$open()})
in a Shiny \code{\link[bslib:input_code_editor]{bslib::input_code_editor()}} that stays in sync with the live
document in both directions, blocks until the editor closes, then prints a
one-line summary.
}
\details{
As you type, the minimal diff is written to the live document and pushed
(debounced); when the text changes remotely, the editor updates to the
merged result. There is no \strong{Save} button -- every edit is applied live. It
syncs whole-text snapshots, not granular operations, so a remote edit
arriving in the brief window between a keystroke and its debounced push can
be overwritten by that push; a smaller \code{debounce} narrows the window. The
original's trailing-newline state is preserved.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
conn <- autosync::sync_client("wss://quarto-hub.com/ws")
doc <- conn$open_doc("4F63WJPDzbHkkfKa66h1Qrr1sC5U")
project_edit(doc, at = "text", ext = ".md")
conn$close()
\dontshow{\}) # examplesIf}
}
