% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sync.R
\name{sync_inputs}
\alias{sync_inputs}
\title{Synchronize Shiny inputs across sessions}
\usage{
sync_inputs(
  session = shiny::getDefaultReactiveDomain(),
  doc_id = "default",
  include = NULL,
  exclude = NULL,
  path = NULL
)
}
\arguments{
\item{session}{The Shiny session object. Default uses
\code{\link[shiny:domains]{shiny::getDefaultReactiveDomain()}}.}

\item{doc_id}{Document identifier string. All sessions using the same
\code{doc_id} share synchronized input state. Default is \code{"default"}.}

\item{include}{Optional character vector of input IDs to synchronize. If
\code{NULL} (default), all eligible inputs are synchronized.}

\item{exclude}{Optional character vector of input IDs to exclude from
synchronization. Applied after \code{include}.}

\item{path}{Optional file path for persistent state. When provided, the
Automerge document is saved to this file after every change and
reloaded automatically when the app restarts. Set to \code{NULL} (default)
for in-memory only.}
}
\value{
A \code{reactiveVal} (logical). \code{FALSE} during normal
operation, \code{TRUE} during replay. Can be ignored if replay is not
used. Called primarily for side effects.
}
\description{
Makes Shiny inputs collaborative by synchronizing their values across
all connected sessions using Automerge CRDT, without requiring an
external sync server.
}
\details{
Call this function once in your Shiny server logic to enable collaborative
input synchronization. Scalar string, numeric, and logical input values
are synchronized automatically. Complex inputs (file uploads, action
buttons, data table selections, etc.) are excluded by default.

When a user changes an input, the new value is propagated to all other
connected sessions. The entire app state is stored in a single Automerge
document, providing automatic conflict resolution via CRDT.

Inputs with names starting with \code{"."} (Shiny internal inputs) are always
excluded.
\subsection{Persistence}{

When \code{path} is provided, the shared document is saved to disk (via
\code{\link[automerge:am_save]{automerge::am_save()}}) after every change. On the next app startup,
the document is loaded from disk and all sessions resume from the
saved state. This makes the shared input state survive R process
restarts.
}
}
\note{
This function synchronizes sessions within a single R process.
For multi-process deployments, use an external sync server with the
\code{\link[=editor]{editor()}} widget instead.
}
\examples{
if (interactive()) {
  ui <- shiny::fluidPage(
    shiny::selectInput("dist", "Distribution",
      c("Normal", "Uniform", "Exponential")),
    shiny::sliderInput("n", "Observations", 10, 500, 100),
    shiny::plotOutput("plot")
  )
  server <- function(input, output, session) {
    sync_inputs()
    output$plot <- shiny::renderPlot(
      hist(switch(input$dist,
        Normal = rnorm(input$n),
        Uniform = runif(input$n),
        Exponential = rexp(input$n)
      ))
    )
  }
  shiny::shinyApp(ui, server)
}

}
\seealso{
Other sync: 
\code{\link{replay_server}()},
\code{\link{replay_ui}()}
}
\concept{sync}
