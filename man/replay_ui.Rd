% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/replay.R
\name{replay_ui}
\alias{replay_ui}
\title{Replay timeline UI}
\usage{
replay_ui(id, show_messages = TRUE, playback_ms = 1000)
}
\arguments{
\item{id}{Module ID.}

\item{show_messages}{Whether to display commit messages below the timeline.
Default is \code{TRUE}.}

\item{playback_ms}{Interval in milliseconds between steps during animated
playback. Default is \code{1000}.}
}
\value{
A Shiny UI element.
}
\description{
Creates a timeline control for replaying the history of synchronized inputs
recorded by \code{\link[=sync_inputs]{sync_inputs()}}.
}
\examples{
if (interactive()) {
  ui <- shiny::fluidPage(
    shiny::selectInput("dist", "Distribution",
      c("Normal", "Uniform", "Exponential")),
    shiny::sliderInput("n", "Observations", 10, 500, 100),
    shiny::plotOutput("plot"),
    replay_ui("timeline")
  )
  server <- function(input, output, session) {
    replaying <- sync_inputs()
    replay_server("timeline", replaying = replaying)
    output$plot <- shiny::renderPlot(
      hist(switch(input$dist,
        Normal = rnorm(input$n),
        Uniform = runif(input$n),
        Exponential = rexp(input$n)
      ))
    )
  }
  shiny::shinyApp(ui, server)
}

}
\seealso{
Other sync: 
\code{\link{replay_server}()},
\code{\link{sync_inputs}()}
}
\concept{sync}
